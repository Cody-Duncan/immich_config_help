# This file is based on https://github.com/immich-app/immich/blob/main/docker/example.env
# and https://documentation.immich.app/docs/install/unraid

# You can find documentation for all the supported env variables at https://immich.app/docs/install/environment-variables

# The location where your uploaded files are stored
UPLOAD_LOCATION=/mnt/user/images/immich                 # * modified per instructions from https://documentation.immich.app/docs/install/unraid

# The Immich version to use. You can pin this to a specific version like "v1.71.0"
IMMICH_VERSION=release

# Connection secrets for postgres and typesense. You should change these to random passwords
TYPESENSE_API_KEY=<MY_SECRET_PASSWORD>                  # * secret for typesense
DB_PASSWORD=<MY_SECRET_PASSWORD>                        # * password for postgres

# The values below this line do not need to be changed
###################################################################################
DB_HOSTNAME=immich_postgres
DB_USERNAME=postgres
DB_DATABASE_NAME=immich

REDIS_HOSTNAME=immich_redis
